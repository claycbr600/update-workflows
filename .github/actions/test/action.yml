name: Action Test

inputs:
  repos:
    required: false
    default: "['developer-portal','intellum-ecommerce','evolve','exceed','level','portal','tribe','hal-9000','devops']"

runs:
  using: composite

  steps:
    - name: Input test
      uses: actions/github-script@v5
      id: input-test
      with:
        script: |
          const repos = ${{ fromJSON(inputs.repos) }}
          console.log("repos = %o", repos)
